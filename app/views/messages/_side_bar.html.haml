.wrapper__side-bar
  .wrapper__side-bar__user-name
    .wrapper__top-items
      .top-items__name
        = current_user.name
      .top-items__icons
        =link_to edit_user_path(current_user), class: 'link-items' do
          %i.fas.fa-cog
        =link_to new_group_path, class: 'link-items' do
          %i.fa.fa-edit 
  
  .wrappr__side-bar__groups-list
    - current_user.groups.each do |group|
      .groups
        =link_to '#' do
          .group-name
            = group.name
          .group-message
            まだメッセージがありません
          